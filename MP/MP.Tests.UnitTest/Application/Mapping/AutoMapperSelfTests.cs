namespace MP.Tests.UnitTest.Application.Mapping
{
    public class AutoMapperSelfTests
    {
        [Fact]
        public void AutoMapper_Configuration_Must_Be_Correct()
        {
            /*
            //arrange
            MapperConfiguration configuration = new(cfg =>
            {
                cfg.AddProfile(new EntityToModelProfile());
                cfg.AddProfile(new ModelToEntityProfile());
            });

            //act
            IMapper _mapper = new Mapper(configuration);

            //assert
            _mapper.ConfigurationProvider.AssertConfigurationIsValid();
            */
        }
    }
}